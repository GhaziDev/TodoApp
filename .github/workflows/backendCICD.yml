name: backend-Todo
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Todo Test Cases
      run: python manage.py test | cd backend
    - name: logging test
      run: echo "Test Logging Succeeded"
    - name: logging pwd
      run: echo pwd

